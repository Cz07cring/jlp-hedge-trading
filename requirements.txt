# ===========================================
# JLP Hedge Trading - Python Dependencies
# ===========================================

# HTTP 客户端
httpx>=0.25.0

# Web3 签名 (EVM)
web3>=6.0.0
eth-account>=0.10.0

# Solana HMAC 签名
pynacl>=1.5.0

# 数据验证
pydantic>=2.0.0

# 异步调度
asyncio-throttle>=1.0.0

# 日志（可选，使用标准 logging 也可）
# loguru>=0.7.0

# 净值报告 (可选)
pandas>=2.0.0
matplotlib>=3.7.0
seaborn>=0.12.0

# 配置解析
python-dotenv>=1.0.0
